# Assessment evidence

Kept separate from `docs/` so the repository reads as a working engineering
artefact rather than a portfolio exercise. Everything here is for the
end-point assessment; nothing here is needed to run or operate the platform.

- `ksb-breadth.md` — the 300-word technical breadth analysis the assessment
  plan requires

## To add before submission

- [ ] Architecture diagram as an image (the plan asks for a diagram artefact;
      `../docs/architecture.md` has the ASCII version to work from)
- [ ] Screenshots: dashboard with the custom metrics populated, before and after
- [ ] The Dependabot chain: PR → CI run → auto-merge → new Lambda version in prod
- [ ] axe DevTools accessibility report for the UI
- [ ] A pipeline run showing a commit reaching the end user
- [ ] Written confirmation from BCS of the CDK Python change to the signed-off mapping
